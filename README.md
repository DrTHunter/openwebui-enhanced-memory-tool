# openwebui-enhanced-memory-tool
Enhanced Memory Enhancement Tool for Open WebUI: adds memory categories/tags, batch ops for speed, smart duplicate detection, and content-based search. Includes clear rules on what the assistant should store (stable info/projects) and never store (passwords/tokens).
